import { StyleSheet } from 'react-native';
import LoginDetails from './logindetails';
const LoginWrapper = ({ navigation }) => {
  return <LoginDetails navigation={navigation} />;
};

export default LoginWrapper;
const styles = StyleSheet.create({});
